# Reflection: TDD รอบ Quantity / Sum

## 1. มี​เทสต์​ทั้งหมด​กี่​ตัว​และ​โค้ด​จริง​กี่​บรรทัด​อัตราส่วน​เป็น​เท่าใด


มี 8 test functions และรันผ่านทั้งหมด โดยได้ 8 passed ส่วนโค้ดจริงใน
`kitchen.py` มี **41 บรรทัดที่ไม่ว่างและไม่ใช่คอมเมนต์** (นับรวมบรรทัดประกาศ
class/def และบรรทัดควบคุมการทำงาน) ดังนั้นอัตราส่วนแบบง่ายคือ **8:41** หรือราว
**1 เทสต์ต่อ 5.1 บรรทัดโค้ดจริง**

ตัวเลขนี้ไม่ได้แปลว่าโค้ดมีคุณภาพเพียงเพราะเทสต์เยอะหรือน้อย: หลายเทสต์มีหลาย
assert และบรรทัดของโค้ดจริงบางส่วนเป็นโครงสร้างภาษา Python ไม่ใช่ตรรกะธุรกิจ
โดยตรง สิ่งที่มีประโยชน์กว่าคือเทสต์ทั้ง 8 ตัวช่วยบอกพฤติกรรมสำคัญได้จริง ได้แก่
การคูณโดยไม่แก้วัตถุเดิม, ความเท่ากันที่คำนึงถึงหน่วย, การบวก, การแปลง oz เป็น g
และการคูณผลบวก

## 2. ช่วง​ที่​แถบ​เป็น​สีแดง​นาน​ที่สุด​คือ​เมื่อใด​อะไร​ทำให้​มัน​นาน

ช่วงที่นานที่สุดคือจาก commit A6 RED: represent addition as Sum ถึง A6: reduce Sum into Quantity, green ใช้เวลาประมาณ 21 นาที

ช่วงนี้นานเพราะปัญหาไม่ใช่แค่ “ทำให้ 200 + 300 ได้ 500” อีกแล้ว ตอนแรกใช้ Fake
It ให้ `plus` คืน `Quantity(500, "g")` ได้ แต่เมื่อมองว่าผลบวกควรยังเก็บ operand
ซ้ายและขวาไว้เพื่อรองรับการ reduce และการแปลงหน่วย จึงต้องเปลี่ยนรูปแทนผลลัพธ์เป็น
`Sum` แล้วหาที่เหมาะสมให้ความรับผิดชอบในการ reduce อยู่ การที่แถบแดงนานจึงสะท้อนว่า
กำลังค้นหาแบบจำลองของโดเมน ไม่ใช่ความล้มเหลวที่ควรรีบกลบด้วยโค้ดมากๆ

## 3. หา​จุด​ที่​ใช้ Fake It หนึ่ง​จุด​ใช้ Triangulate หนึ่ง​จุด​และ​ใช้ Obvious Implementation หนึ่ง​จุด​เมื่อ​มอง​ย้อนกลับ​ไป​แต่ละครั้ง​เลือก​ได้​เหมาะสม​หรือไม่


* **Fake It — A6:** `Quantity.plus` เคยคืน `Quantity(500, "g")` ตายตัว เพื่อให้
  เทสต์การบวกตัวแรกผ่านเร็วที่สุด การเลือกนี้เหมาะในตอนนั้นเพราะเป็นการทดลองว่า API
  `plus` และ `Converter.reduce` ควรหน้าตาอย่างไร แต่จะไม่เหมาะทันทีเมื่อมีอินพุตอื่น;
  โค้ดปลอมจึงถูกแทนด้วย `Sum` ในรอบถัดมา ไม่ได้ถูกปล่อยค้างไว้เป็นหนี้เทคนิค
* **Triangulate — A1/A2:** หลังจากกรณีแรกทำให้ `times(3)` ได้ 600 ด้วยค่าตายตัว
  เทสต์กรณีที่สอง `times(2)` ได้ 400 บังคับให้เลิก hard-code และสรุปกฎทั่วไปเป็น
  `amount * multiplier` นี่เหมาะ เพราะสองตัวอย่างที่ต่างกันช่วยยืนยันว่ากำลังหา
  “กฎการคูณ” ไม่ใช่เพียงทำให้ตัวอย่างแรกผ่าน
* **Obvious Implementation — A5:** เทสต์ `1 g != 1 oz` ทำให้เพิ่มการเปรียบเทียบ
  `unit` ใน `Quantity.__eq__` นี่เป็นการ implement ตรงไปตรงมาที่เหมาะสม เพราะกฎของ
  quantity ระบุชัดว่าความเท่ากันต้องมีทั้งจำนวนและหน่วย ไม่ต้องสร้าง abstraction
  หรือ fake เพิ่ม

## 4. ยังมี​รายการ​ใด​ค้าง​อยู่​ใน​รายการ​ทดสอบ​รายการ​ใด​ที่จะ​ลงมือทำ​จริง​และ​รายการ​ใด​ที่​ควร​ลบทิ้ง​เพราะ​เป็นการ​คาดเดา​ล่วงหน้า

ตอนนี้ไม่มีรายการค้างใน checklist แล้ว เพราะทั้ง 7 ข้อที่กำหนดไว้ทำเสร็จและเทสต์ผ่านทั้งหมด

ส่วนสิ่งที่อาจทำเพิ่ม เช่น การแปลงจาก g เป็น oz หรือรองรับหน่วยอื่น ๆ ยังไม่มีอยู่ใน requirements ดังนั้นตอนนี้ยังไม่ควรรีบทำ เพราะอาจทำให้โค้ดซับซ้อนเกินความจำเป็น ถ้ามี requirement เพิ่มค่อยเพิ่มเทสต์แล้วพัฒนาต่อ

## 5. ดู​เมท็อด Quantity.reduce ถ้า​ออกแบบ​ระบบ​ทั้งหมด​บน​กระดาษ​ก่อนนักศึกษาจะ​เขียน​เมท็อด​นี้​ออกมา​หรือไม่

ข้อสังเกตที่สำคัญคือโค้ดปัจจุบัน **ยังไม่มี `Quantity.reduce`**; มี
`Converter.reduce` และ `Sum.reduce` เท่านั้น ดังนั้นถ้าเรียก
`Converter().reduce(grams(200), "g")` ตอนนี้จะใช้ไม่ได้ แม้การ reduce `Sum` จะผ่าน
เทสต์แล้ว นี่เป็นช่องว่างจริงของแบบออกแบบปัจจุบัน ไม่ควรอ้างว่า API สมบูรณ์เกินกว่า
ที่เทสต์พิสูจน์ได้

หากออกแบบทั้งระบบบนกระดาษก่อน มีโอกาสสูงที่จะใส่ `Quantity.reduce` ตั้งแต่แรกเพื่อให้
Quantity และ Sum มี interface เดียวกัน แต่ใน TDD ยังไม่มีเทสต์ที่ต้อง reduce
Quantity โดยตรง จึงยังไม่มีหลักฐานพอให้เขียนเมท็อดนั้น การชะลอไว้เหมาะกับวงจรนี้:
เมื่อมีเทสต์ดังกล่าว จึงค่อยเพิ่ม implementation ที่เล็กที่สุด (กรณีหน่วยเดียวกันอาจ
คืนตัวเอง; กรณีต่างหน่วยค่อยใช้ `Converter`) วิธีนี้ยอมรับความไม่สมบูรณ์ชั่วคราว
เพื่อหลีกเลี่ยงการออกแบบจากการเดา และให้เทสต์ถัดไปเป็นผู้บอกว่า abstraction ที่ควรมี
จริงๆ คืออะไร
